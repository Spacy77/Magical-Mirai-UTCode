## Règles

- les lettres d'un même mot sont alignées ;
- on récupère lettre par lettre, et non mot par mot ;

## Project structure (extracted)

- Root: [index.html](index.html) — minimal HTML that loads libraries and the app.
- App sources: src/styles.css, src/app.js — main CSS and JavaScript (extracted for easier editing).

Edit `src/app.js` and `src/styles.css` during development. Open the root `index.html` to run the project in a browser.